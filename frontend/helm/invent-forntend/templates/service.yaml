apiVersion: v1
kind: Service
metadata:
  name: {{ include "invent-forntend.fullname" . }}
  labels:
    {{- include "invent-forntend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "invent-forntend.selectorLabels" . | nindent 4 }}
